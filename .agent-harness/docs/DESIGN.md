# Design

This base repository should be simple, legible, and practical.

Design work here means designing the agent operating system: docs, templates,
runbooks, validation output, and proof-of-work formats.

Product-specific UI or brand design belongs in the target product repository,
but reusable UI/UX guidance for future products lives in `.agent-harness/docs/UI_UX.md`.
