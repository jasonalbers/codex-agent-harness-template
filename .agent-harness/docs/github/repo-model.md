# GitHub Repository Model

Each product or project has its own GitHub repository.

The base repository stores process and orchestration guidance. Product code
belongs in the configured target repositories.

Project config lives in `.agent-harness/config/projects.json` or starts from
`.agent-harness/config/projects.example.json`.
